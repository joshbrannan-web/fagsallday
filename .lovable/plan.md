

# Add Google Service Account Secret

## Single Step
Use the `add_secret` tool to prompt you to paste your Google Service Account JSON key as `GOOGLE_SERVICE_ACCOUNT_KEY`. This will make it available to the edge functions that create and sync Google Sheets.

No code or database changes needed — just the secret configuration.

