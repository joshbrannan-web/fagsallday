

## Remove Temporary Grant Tournament Admin Button

### Change
**`src/pages/Profile.tsx`** — Remove the temporary "Grant Tournament Admin" section (lines 282-316): the dashed-border div containing the button and its inline click handler. No other code is touched. The `tournament_admins` table and its data remain intact.

